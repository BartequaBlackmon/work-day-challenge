console.log("javescripts loaded")
// with jQuery, grab all time-blocks [9am-time-block, 10am-time-block, ...]
// use moment to get the current hour
// for each element, compare it against current time and add appropriate class

// if there's items in localstorage, add values to corresponding hour element

// event listener for saving text to localstorage